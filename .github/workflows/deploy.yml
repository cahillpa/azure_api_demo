name: Build and Deploy Azure Function

on:
  push:
    branches:
      - main
      - release1
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build and package with Maven
        run: mvn clean package azure-functions:package

      - name: List deployment folder contents (debug)
        run: ls -R target/azure-functions/azure-api-demo-function

      - name: Zip function app files
        run: |
          cd target/azure-functions/azure-api-demo-function
          zip -r ../../functionapp.zip .

      - name: Deploy to Azure Functions
        uses: azure/functions-action@v1
        with:
          app-name: azure-api-demo-function
          package: target/functionapp.zip
          publish-profile: ${{ secrets.PUBLISH_AZURE_FUNCTIONS }}
